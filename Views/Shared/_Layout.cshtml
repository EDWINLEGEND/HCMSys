@{
    Layout = null;
    var currentController = ViewContext.RouteData.Values["controller"]?.ToString() ?? "";
    var currentAction = ViewContext.RouteData.Values["action"]?.ToString() ?? "";
}

@using Microsoft.AspNetCore.Http;
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>
        HCMSys
       
 
    </title>
    <meta content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" name="viewport">
 
    <!-- Custom fonts for this template-->
    <link href="~/dist/fontawesome-free/css/all.min.css" rel="stylesheet" type="text/css">
    <link href="~/dist/fontawesome-free/css/brands.min.css" rel="stylesheet" type="text/css">
    <link href="https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i"
          rel="stylesheet">
    <link href="~/dist/css/sb-admin-2.min.css" rel="stylesheet">


    <!-- Custom styles for this template-->
    <link href="~/dist/css/sb-admin-2.min.css" rel="stylesheet">
    <style>
        body:not(.modal-open) .modal-backdrop { display: none !important; }
        .modal-backdrop.show:not(.modal.show ~ .modal-backdrop) { pointer-events: none !important; }
        #main-page-container { transition: opacity 0.15s ease-in-out; }
    </style>

    <!-- Bootstrap core JavaScript-->
    <script src="~/dist/jquery/jquery.min.js"></script>
    <script src="~/dist/bootstrap/js/bootstrap.bundle.min.js"></script>

    <!-- Core plugin JavaScript-->
    <script src="~/dist/jquery-easing/jquery.easing.min.js"></script>

    <!-- Custom scripts for all pages-->
    <script src="~/dist/js/sb-admin-2.min.js"></script>

    <!-- Page level plugins -->
    <script src="~/dist/chart.js/Chart.min.js"></script>
    <script src="~/HCM/common.js"></script>
    <script src="~/dist/fontawesome-free/js/all.min.js"></script>
    <script src="~/dist/fontawesome-free/js/brands.min.js"></script>
    <link href="~/lib/datepicker/datepicker.css" rel="stylesheet" />
    <script defer src="~/lib/datatables/js/jquery.dataTables.min.js"></script>
    <link href="~/lib/datatables/css/jquery.dataTables.css" rel="stylesheet" />
    <script src="~/lib/select2/js/select2.min.js"></script>
    <link href="~/lib/select2/css/select2.min.css" rel="stylesheet" />
    <style>
        /* Global Select2 Styling matching SB Admin 2 & Modern Design */
        .select2-container--default .select2-selection--single {
            height: calc(1.5em + .75rem + 2px) !important;
            border: 1px solid #d1d3e2 !important;
            border-radius: 0.35rem !important;
            background-color: #fff !important;
            display: flex !important;
            align-items: center !important;
        }
        .select2-container--default .select2-selection--single .select2-selection__rendered {
            line-height: calc(1.5em + .75rem) !important;
            color: #6e707e !important;
            padding-left: 0.75rem !important;
            padding-right: 1.75rem !important;
            font-size: 0.85rem !important;
        }
        .select2-container--default .select2-selection--single .select2-selection__arrow {
            height: calc(1.5em + .75rem) !important;
            top: 0 !important;
            right: 5px !important;
        }
        .select2-container--default .select2-selection--single .select2-selection__placeholder {
            color: #858796 !important;
        }
        .select2-dropdown {
            border: 1px solid #d1d3e2 !important;
            border-radius: 0.35rem !important;
            box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.15) !important;
            z-index: 9999 !important;
        }
        .select2-container--default .select2-search--dropdown .select2-search__field {
            border: 1px solid #d1d3e2 !important;
            border-radius: 0.25rem !important;
            padding: 6px 10px !important;
            font-size: 0.85rem !important;
            outline: none !important;
        }
        .select2-container--default .select2-results__option {
            padding: 8px 12px !important;
            font-size: 0.85rem !important;
            color: #5a5c69 !important;
        }
        .select2-container--default .select2-results__option--highlighted[aria-selected] {
            background-color: #4e73df !important;
            color: #ffffff !important;
        }
        .select2-container--default .select2-results__option[aria-selected="true"] {
            background-color: #eaecf4 !important;
            color: #2e59d9 !important;
            font-weight: 600 !important;
        }
        .select2-container .select2-selection--multiple {
            min-height: calc(1.5em + .75rem + 2px) !important;
            border: 1px solid #d1d3e2 !important;
            border-radius: 0.35rem !important;
        }
    </style>
    <script src="~/alertify.js/alertify.min.js"></script>
    <script>
        if (typeof alertify !== 'undefined') {
            alertify.set({ delay: 5000 });
        }
        window.alert = function (msg) {
            if (typeof alertify !== 'undefined') {
                alertify.set({ delay: 5000 });
                alertify.log(String(msg), 'error', 5000);
            } else {
                console.warn('Alert intercepted:', msg);
            }
        };
    </script>
    <link rel="stylesheet" href="~/alertify.js/alertify.core.css?v=3" />
    <link rel="stylesheet" href="~/alertify.js/alertify.default.css" />
    <script defer src="~/datatables.net-rowgroup/dataTables.rowGroup.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
@*    <script src="js/demo/chart-area-demo.js"></script>
    <script src="js/demo/chart-pie-demo.js"></script>*@
    <script>
        var display = setInterval(function () { Time() }, 0);
        $('[data-widget="pushmenu"]').on('click', function () {
            $('[data-widget="pushmenu"]').PushMenu("toggle");
        });
        $(document).ready(function () {
            // Show scroll-to-top button after scrolling down 100px
            $(window).scroll(function () {
                if ($(this).scrollTop() > 100) {
                    $('.scroll-to-top').fadeIn();
                } else {
                    $('.scroll-to-top').fadeOut();
                }
            });

            // Smooth scroll to top on click
            $('.scroll-to-top').click(function () {
                $('html, body').animate({ scrollTop: 0 }, 600);
                return false;
            });
        });

        function Time() {
            var _date = new Date();
            var _time = _date.toLocaleDateString("es-CL") + ' ' + _date.toLocaleTimeString().toUpperCase();
 
            document.getElementById("eg").innerHTML = _time;
            // $('#eg').val(_time);
        }
        document.addEventListener("DOMContentLoaded", function () {
 

            function getTime() {
                return new Date().toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });
            }

 
 
        });
    </script>
 
    <style>
        #eg {
            font-family: Segoe UI;
            font-size: 24px;
            color: white;
        }

        ;
        #hd {
            font-family: Segoe UI;
            font-size: 24px;
            color: white;
        };
        html {
    font-size: 14px; /* Default is usually 16px */
}
    </style>
</head>
<body class="hold-transition skin-black sidebar-mini-expand-feature " >
    <script>
        if (localStorage.getItem('sidebar_toggled') === 'true') {
            document.body.classList.add('sidebar-toggled');
        }
    </script>
   <!-- Page Wrapper -->
    <div id="wrapper">
        
        <!-- Sidebar -->
        <ul class="navbar-nav sidebar sidebar-dark accordion" id="accordionSidebar" style="background:#2C3333">
            <script>
                if (localStorage.getItem('sidebar_toggled') === 'true') {
                    document.getElementById('accordionSidebar').classList.add('toggled');
                }
            </script>
            <!-- Sidebar - Brand -->
            <a class="sidebar-brand d-flex align-items-center justify-content-center" href="@Url.Action("Index", "Home")">
                <div class="sidebar-brand-icon rotate-n-15">
                    <i class="fas fa-fw fa-calendar-alt"></i>
                </div>
                <div class="sidebar-brand-text mx-3">HCM<sup>Sys</sup></div>
            </a>

            <!-- Divider -->
            <hr class="sidebar-divider my-0">

            <!-- Nav Item - Dashboard -->
            <li class="nav-item @(currentController == "Home" && currentAction == "Index" ? "active" : "")">
                <a class="nav-link" href="@Url.Action("Index", "Home")" id="mnuDashboard">
                    <i class="fas fa-fw fa-home"></i>
                    <span>Dashboard</span>
                </a>
            </li>
 
            @{
                var isMastersActive = currentAction.StartsWith("Project");
                var isLoansActive = currentAction.StartsWith("Loan");
                var isLeavesActive = currentAction.StartsWith("Leave") || currentAction.StartsWith("OpeningLeaves");
                var isHrActive = currentAction.StartsWith("Claim") || currentAction.StartsWith("BusinessTrip") || currentAction.StartsWith("HR");
                var isAssetsActive = currentAction.StartsWith("Asset");
            }

            <!-- Masters Group -->
            <li class="nav-item @(isMastersActive ? "active" : "")">
                <a class="nav-link @(isMastersActive ? "" : "collapsed")" href="#" data-toggle="collapse" data-target="#collapseMasters"
                   aria-expanded="@(isMastersActive ? "true" : "false")" aria-controls="collapseMasters">
                    <i class="fas fa-fw fa-cubes"></i>
                    <span>Masters</span>
                </a>
                <div id="collapseMasters" class="collapse @(isMastersActive ? "show" : "")" data-parent="#accordionSidebar">
                    <div class="bg-white py-2 collapse-inner rounded">
                        <a class="collapse-item @(currentAction.StartsWith("Project") ? "active" : "")" href="@Url.Action("ProjectIndex", "Master")">
                            <i class="fas fa-fw fa-project-diagram mr-2 text-gray-500"></i>Project
                        </a>
                    </div>
                </div>
            </li>

            <!-- Loan Management Group -->
            <li class="nav-item @(isLoansActive ? "active" : "")">
                <a class="nav-link @(isLoansActive ? "" : "collapsed")" href="#" data-toggle="collapse" data-target="#collapseLoans"
                   aria-expanded="@(isLoansActive ? "true" : "false")" aria-controls="collapseLoans">
                    <i class="fas fa-fw fa-hand-holding-usd"></i>
                    <span>Loan Management</span>
                </a>
                <div id="collapseLoans" class="collapse @(isLoansActive ? "show" : "")" data-parent="#accordionSidebar">
                    <div class="bg-white py-2 collapse-inner rounded">
                        <a class="collapse-item @(currentAction.StartsWith("LoanRequest") ? "active" : "")" href="@Url.Action("LoanRequestIndex", "Master")">Loan Request</a>
                        <a class="collapse-item @(currentAction.StartsWith("LoanDisbursement") ? "active" : "")" href="@Url.Action("LoanDisbursementIndex", "Master")">Loan Disbursement</a>
                        <a class="collapse-item @(currentAction.StartsWith("LoanDeferment") ? "active" : "")" href="@Url.Action("LoanDefermentIndex", "Master")">Loan Deferment</a>
                        <a class="collapse-item @(currentAction.StartsWith("LoanSettlement") ? "active" : "")" href="@Url.Action("LoanSettlementIndex", "Master")">Loan Settlement</a>
                    </div>
                </div>
            </li>

            <!-- Leave Operations Group -->
            <li class="nav-item @(isLeavesActive ? "active" : "")">
                <a class="nav-link @(isLeavesActive ? "" : "collapsed")" href="#" data-toggle="collapse" data-target="#collapseLeaves"
                   aria-expanded="@(isLeavesActive ? "true" : "false")" aria-controls="collapseLeaves">
                    <i class="fas fa-fw fa-calendar-alt"></i>
                    <span>Leave Operations</span>
                </a>
                <div id="collapseLeaves" class="collapse @(isLeavesActive ? "show" : "")" data-parent="#accordionSidebar">
                    <div class="bg-white py-2 collapse-inner rounded">
                        <a class="collapse-item @(currentAction.StartsWith("LeaveApplication") ? "active" : "")" href="@Url.Action("LeaveApplicationIndex", "Master")">Leave Application</a>
                        <a class="collapse-item @(currentAction.StartsWith("OpeningLeaves") ? "active" : "")" href="@Url.Action("OpeningLeavesIndex", "Master")">Opening Leaves</a>
                        <a class="collapse-item @(currentAction.StartsWith("LeaveEligibility") ? "active" : "")" href="@Url.Action("LeaveEligibilityIndex", "Master")">Leave Eligibility</a>
                        <a class="collapse-item @(currentAction.StartsWith("LeaveAdjustments") ? "active" : "")" href="@Url.Action("LeaveAdjustmentsIndex", "Master")">Leave Adjustments</a>
                    </div>
                </div>
            </li>

            <!-- HR & Expenses Group -->
            <li class="nav-item @(isHrActive ? "active" : "")">
                <a class="nav-link @(isHrActive ? "" : "collapsed")" href="#" data-toggle="collapse" data-target="#collapseHR"
                   aria-expanded="@(isHrActive ? "true" : "false")" aria-controls="collapseHR">
                    <i class="fas fa-fw fa-user-cog"></i>
                    <span>HR & Expenses</span>
                </a>
                <div id="collapseHR" class="collapse @(isHrActive ? "show" : "")" data-parent="#accordionSidebar">
                    <div class="bg-white py-2 collapse-inner rounded">
                        <a class="collapse-item @(currentAction.StartsWith("Claim") ? "active" : "")" href="@Url.Action("ClaimIndex", "Master")">Claims</a>
                        <a class="collapse-item @(currentAction.StartsWith("BusinessTrip") ? "active" : "")" href="@Url.Action("BusinessTripIndex", "Master")">Business Trip</a>
                        <a class="collapse-item @(currentAction.StartsWith("HRRequest") ? "active" : "")" href="@Url.Action("HRRequestIndex", "Master")">HR Request</a>
                        <a class="collapse-item @(currentAction.StartsWith("HRReturn") ? "active" : "")" href="@Url.Action("HRReturnIndex", "Master")">HR Return</a>
                        <a class="collapse-item @(currentAction.StartsWith("HRDocumentIssue") ? "active" : "")" href="@Url.Action("HRDocumentIssueIndex", "Master")">HR Document Issue</a>
                    </div>
                </div>
            </li>

            <!-- Asset Management Group -->
            <li class="nav-item @(isAssetsActive ? "active" : "")">
                <a class="nav-link @(isAssetsActive ? "" : "collapsed")" href="#" data-toggle="collapse" data-target="#collapseAssets"
                   aria-expanded="@(isAssetsActive ? "true" : "false")" aria-controls="collapseAssets">
                    <i class="fas fa-fw fa-boxes"></i>
                    <span>Asset Management</span>
                </a>
                <div id="collapseAssets" class="collapse @(isAssetsActive ? "show" : "")" data-parent="#accordionSidebar">
                    <div class="bg-white py-2 collapse-inner rounded">
                        <a class="collapse-item @(currentAction.StartsWith("AssetAllocation") ? "active" : "")" href="@Url.Action("AssetAllocationIndex", "Master")">Asset Allocation</a>
                        <a class="collapse-item @(currentAction.StartsWith("AssetDeAllocation") ? "active" : "")" href="@Url.Action("AssetDeAllocationIndex", "Master")">Asset De-Allocation</a>
                    </div>
                </div>
            </li>

 
         
            <!-- Divider -->
            <hr class="sidebar-divider d-none d-md-block">
 
            <!-- Sidebar Toggler (Sidebar) -->
            <div class="text-center d-none d-md-inline">
                <button class="rounded-circle border-0" id="sidebarToggle"></button>
            </div>
        </ul>
        <!-- End of Sidebar -->

        <!-- Content Wrapper -->
        <div id="content-wrapper" class="d-flex flex-column">

            <!-- Main Content -->
            <div id="content">

                <!-- Topbar -->
                <nav class="navbar navbar-expand navbar-light bg-white topbar mb-4 static-top shadow">
 
                  <!-- Topbar Search -->
                    <form
                        class="d-none d-sm-inline-block form-inline mr-auto ml-md-3 my-2 my-md-0 mw-100 navbar-search">
                        <div class="input-group">
                            <h6 class="h3 mb-0 text-gray-800" id="eg"> TIME</h6>
                        </div>
                    </form>
 

                </nav>
                <!-- End of Topbar -->
 
                <!-- Begin Page Content -->
                <div class="container-fluid" id="main-page-container" style="transition: opacity 0.15s ease-in-out;">

                    <div class="row">


                        @RenderBody()
                        

                    </div>

                </div>
                <!-- /.container-fluid -->

            </div>
 
            <!-- End of Footer -->

        </div>
        <!-- End of Content Wrapper -->

    </div>
    <!-- End of Page Wrapper -->

    <!-- Scroll to Top Button-->
    <a class="scroll-to-top rounded" href="#" onclick="scrollToTop(event)">
        <i class="fas fa-angle-up"></i>
    </a>

    <!-- Logout Modal-->
    <div class="modal fade" id="logoutModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel"
        aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Ready to Leave?</h5>
                    <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">×</span>
                    </button>
                </div>
                <div class="modal-body">Select "Logout" below if you are ready to end your current session.</div>
                <div class="modal-footer">
                    <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</button>
                    <a class="btn btn-primary" href="@Url.Action("Index", "Login")">Logout</a>
                </div>
            </div>
        </div>
    </div>

    <script>
        function updateActiveSidebar(targetUrl) {
            var currentPath = new URL(targetUrl || window.location.href, window.location.origin).pathname.toLowerCase();

            $('#accordionSidebar .nav-item').removeClass('active');
            $('#accordionSidebar .collapse-item').removeClass('active');

            var foundMatch = false;

            $('#accordionSidebar a.nav-link, #accordionSidebar a.collapse-item').each(function () {
                var href = $(this).attr('href');
                if (href && href !== '#' && href !== 'javascript:void(0);' && !$(this).attr('data-toggle')) {
                    try {
                        var linkPath = new URL(href, window.location.origin).pathname.toLowerCase();

                        var cleanCurrent = currentPath.replace(/\/+$/, '').replace(/index$/, '');
                        var cleanLink = linkPath.replace(/\/+$/, '').replace(/index$/, '');

                        var isMatch = (currentPath === linkPath) || 
                                      (cleanCurrent.length > 1 && cleanLink.length > 1 && cleanCurrent === cleanLink);

                        if (isMatch && !foundMatch) {
                            foundMatch = true;
                            if ($(this).hasClass('collapse-item')) {
                                $(this).addClass('active');
                                var parentCollapse = $(this).closest('.collapse');
                                parentCollapse.addClass('show');
                                parentCollapse.prev('a.nav-link').removeClass('collapsed').attr('aria-expanded', 'true');
                                parentCollapse.closest('.nav-item').addClass('active');
                            } else {
                                $(this).closest('.nav-item').addClass('active');
                            }
                        }
                    } catch (e) { }
                }
            });

            if (!foundMatch && (currentPath === '/' || currentPath.endsWith('/home') || currentPath.endsWith('/home/index'))) {
                $('#mnuDashboard').closest('.nav-item').addClass('active');
            }
        }

        function checkPendingAlert() {
            var pendingAlert = sessionStorage.getItem('pendingAlert');
            if (pendingAlert) {
                try {
                    var alertObj = JSON.parse(pendingAlert);
                    if (typeof alertify !== 'undefined') {
                        $('.alertify-log, .alertify-logs > *').remove();
                        alertify.set({ delay: 3000 });
                        alertify.log(alertObj.msg, alertObj.type || 'success', 3000);
                    }
                } catch(e) {}
                sessionStorage.removeItem('pendingAlert');
            }
        }

        $(document).on('click', '.alertify-log', function() {
            $(this).fadeOut(150, function() { $(this).remove(); });
        });

        var activeAjaxRequest = null;

        function fetchWithFallback(apiUrl, fallbackUrl) {
            var def = $.Deferred();
            $.getJSON(apiUrl).done(function(data) {
                if (data && ((Array.isArray(data) && data.length > 0) || (typeof data === 'object' && Object.keys(data).length > 0))) {
                    def.resolve(data);
                } else {
                    $.getJSON(fallbackUrl).done(function(fbData) {
                        def.resolve(fbData);
                    }).fail(function() {
                        def.reject();
                    });
                }
            }).fail(function() {
                $.getJSON(fallbackUrl).done(function(fbData) {
                    def.resolve(fbData);
                }).fail(function() {
                    def.reject();
                });
            });
            return def.promise();
        }

        function applySelect2Everywhere(container) {
            if (typeof $.fn.select2 === 'undefined') return;
            var $target = container ? $(container) : $(document.body);
            $target.find('select:not(.no-select2)').each(function() {
                var $elem = $(this);
                if ($elem.hasClass('select2-hidden-accessible')) {
                    return;
                }
                var placeholderText = $elem.find('option[value=""]').first().text() || $elem.attr('placeholder') || 'Select...';
                var isModal = $elem.closest('.modal').length > 0;
                
                $elem.select2({
                    width: '100%',
                    placeholder: placeholderText,
                    dropdownParent: isModal ? $elem.closest('.modal') : $(document.body)
                });
            });
        }

        window.getDocBlobUrl = function(filename, url) {
            if (url && typeof url === 'string' && url.trim() !== '' && url !== '#' && url !== 'undefined' && url !== 'null') {
                var trimmed = url.trim();
                if (trimmed.startsWith('data:')) {
                    try {
                        var parts = trimmed.split(',');
                        var mimeMatch = parts[0].match(/:(.*?);/);
                        var mime = mimeMatch ? mimeMatch[1] : 'application/octet-stream';
                        var bstr = atob(parts[1]);
                        var n = bstr.length;
                        var u8arr = new Uint8Array(n);
                        while (n--) {
                            u8arr[n] = bstr.charCodeAt(n);
                        }
                        var blob = new Blob([u8arr], { type: mime });
                        return URL.createObjectURL(blob);
                    } catch (e) {
                        console.error('Error converting Data URL to Blob:', e);
                        return trimmed;
                    }
                }
                return trimmed;
            }
            
            var isPdf = (filename && filename.toLowerCase().endsWith('.pdf'));
            var docHtml = `<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8"/>
    <title>${filename || 'Document Preview'}</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css"/>
    <style>
        body { font-family: 'Nunito', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; padding: 60px 20px; background: #f8f9fc; color: #5a5c69; text-align: center; margin: 0; }
        .card { background: #fff; padding: 50px 40px; border-radius: 12px; box-shadow: 0 0.15rem 1.75rem 0 rgba(58,59,69,0.15); display: inline-block; max-width: 600px; width: 100%; border-top: 5px solid #4e73df; }
        h3 { color: #4e73df; margin-top: 0; font-weight: 700; font-size: 1.5rem; }
        .doc-icon { font-size: 4rem; color: #e74a3b; margin-bottom: 20px; }
        .doc-info { background: #eaecf4; padding: 15px; border-radius: 8px; font-weight: 600; word-break: break-all; margin: 20px 0; color: #2e59d9; }
    </style>
</head>
<body>
    <div class="card">
        <div class="doc-icon"><i class="fas ${isPdf ? 'fa-file-pdf' : 'fa-file-alt'}"></i></div>
        <h3>Document Preview</h3>
        <p style="color: #858796;">Viewing document attached to application record.</p>
        <div class="doc-info">${filename || 'Attachment.pdf'}</div>
        <p style="font-size: 0.9rem; color: #858796;">Document opened successfully in a new tab.</p>
    </div>
</body>
</html>`;
            var blob = new Blob([docHtml], { type: 'text/html' });
            return URL.createObjectURL(blob);
        };

        window.openDocPreview = function(filename, url) {
            var blobUrl = window.getDocBlobUrl(filename, url);
            var win = window.open(blobUrl, '_blank');
            if (!win) {
                window.location.href = blobUrl;
            }
        };

        function loadPageContent(url, pushState) {
            if (pushState === undefined) pushState = true;
            
            // Abort previous in-flight navigation if user clicks sidebar items again
            if (activeAjaxRequest && activeAjaxRequest.readyState !== 4) {
                activeAjaxRequest.abort();
            }

            // Force-close any open modals and cleanup lingering backdrops
            try { $('.modal').modal('hide'); } catch(e) {}
            $('.modal-backdrop').remove();
            $('body').removeClass('modal-open').css({'padding-right': '', 'overflow': ''});

            var targetFullUrl = new URL(url, window.location.origin).href;

            activeAjaxRequest = $.ajax({
                url: targetFullUrl,
                type: 'GET',
                headers: { 'X-Requested-With': 'XMLHttpRequest' },
                dataType: 'html',
                success: function (data) {
                    $('.modal-backdrop').remove();
                    $('body').removeClass('modal-open').css({'padding-right': '', 'overflow': ''});

                    var $parsed = $('<div>').append($.parseHTML(data, document, true));

                    // Strip out redundant external library script tags from incoming view HTML so global jQuery/Bootstrap state is never overwritten
                    $parsed.find('script[src]').each(function() {
                        var src = ($(this).attr('src') || '').toLowerCase();
                        if (src.indexOf('jquery') !== -1 || src.indexOf('bootstrap') !== -1 || src.indexOf('sb-admin-2') !== -1 || src.indexOf('fontawesome') !== -1) {
                            $(this).remove();
                        }
                    });

                    var $newContainer = $parsed.find('#main-page-container');

                    if ($newContainer.length > 0) {
                        $('#main-page-container').html($newContainer.html());
                    } else {
                        $('#main-page-container').html($parsed.html());
                    }

                    if (pushState) {
                        history.pushState({ url: targetFullUrl }, '', targetFullUrl);
                    }

                    updateActiveSidebar(targetFullUrl);
                    window.scrollTo({ top: 0, behavior: 'smooth' });
                    checkPendingAlert();
                    setTimeout(function() { applySelect2Everywhere('#main-page-container'); }, 100);
                },
                error: function (xhr, status) {
                    if (status !== 'abort') {
                        window.location.href = targetFullUrl;
                    }
                },
                complete: function () {
                    $('#main-page-container').css('opacity', '1');
                    $('.modal-backdrop').remove();
                    $('body').removeClass('modal-open').css({'padding-right': '', 'overflow': ''});
                }
            });
        }

        $(document).ready(function () {
            // 1. Restore Sidebar Toggle state
            if (localStorage.getItem('sidebar_toggled') === 'true') {
                $('body').addClass('sidebar-toggled');
                $('#accordionSidebar').addClass('toggled');
            }

            $('#sidebarToggle, #sidebarToggleTop').on('click', function () {
                var isToggled = $('#accordionSidebar').hasClass('toggled');
                localStorage.setItem('sidebar_toggled', isToggled);
            });

            // 2. Initial Active Sidebar Item & Pending Notification Check
            updateActiveSidebar(window.location.href);
            checkPendingAlert();
            setTimeout(function() { applySelect2Everywhere(); }, 150);

            // 3. Restore and Persist Sidebar Scroll Position
            var savedScroll = sessionStorage.getItem('sidebar_scroll');
            if (savedScroll !== null) {
                $('#accordionSidebar').scrollTop(parseInt(savedScroll));
            }

            $('#accordionSidebar').on('scroll', function () {
                sessionStorage.setItem('sidebar_scroll', $(this).scrollTop());
            });

            $('#accordionSidebar a').on('click', function () {
                sessionStorage.setItem('sidebar_scroll', $('#accordionSidebar').scrollTop());
            });
        });

        // Global Modal Event Listener: Purge any orphan backdrops when any modal is closed
        $(document).on('hidden.bs.modal', '.modal', function () {
            $('.modal-backdrop').remove();
            $('body').removeClass('modal-open').css({'padding-right': '', 'overflow': ''});
        });

        $(document).on('shown.bs.modal', '.modal', function (e) {
            applySelect2Everywhere(e.target);
        });

        // 4. Intercept Clicks for Dynamic Navigation (Permanent Sidebar)
        $(document).on('click', '#accordionSidebar a.nav-link:not([data-toggle]), #accordionSidebar a.collapse-item, .btn-group-actions a, a.btn, a.spa-link', function (e) {
            var href = $(this).attr('href');
            var isModal = $(this).attr('data-toggle') === 'modal' || $(this).attr('data-target') || $(this).attr('data-dismiss');
            if (!isModal && href && href !== '#' && href !== 'javascript:void(0);' && !href.startsWith('http') && !$(this).attr('target')) {
                e.preventDefault();
                loadPageContent(href, true);
            }
        });

        // 5. Handle Browser Back & Forward Buttons
        window.addEventListener('popstate', function (e) {
            loadPageContent(window.location.href, false);
        });
    </script>
</body>
</html>
